# План наповнення блогу за пропущений період (12.07.2026 — сьогодні)

## Статус

**Перший етап — ЗАВЕРШЕНО, підтверджено на продакшені.** 13 статей
написано, поглиблено реальними деталями з коду/документації, імпортовано в
`blog_posts` (перевірено і локально, і на проді — 53 статті всього,
40 старих + 13 нових). Перша спроба на проді мовчки пропустила імпорт
(staging-файли ще не були скопійовані) — виправлено ручним запуском
`BlogBacklogImportSeeder` через tinker після заливки файлів по FTP.

14-та стаття ("Фото замість суперечок" — техобслуговування) відкладена:
виявилось, що це функція режиму персоналу, а не водія — перенесена в
`workspace/docs/articles/future-staff-mode/maintenance-photos.md`, чекає
на майбутню зміну позиціонування.

## Фінальний список статей

13 статей, без mobile-user, без функцій режиму персоналу в мобільному
застосунку (позиціонування — тільки для водія).

### Заднім числом (заповнюємо липневу прогалину)

| Дата | Заголовок | Суть |
|---|---|---|
| 16.07 | Garage24 тепер і в кишені водія (анонс) | Загальний анонс: у застосунку водія вже графік, облік поїздок, авто, витрати — офіційно представляємо |
| 18.07 | Графік роботи, який завжди під рукою | Водій бачить свій графік у застосунку, без дзвінків диспетчеру |
| 20.07 | Легша синхронізація з Uklon | Старі дані Uklon перенесені в архів — швидша робота, нічого не втрачено |
| 22.07 | Працюй навіть без інтернету | Офлайн-режим — застосунок показує дані навіть у зоні поганого зв'язку |
| 24.07 | SOS у застосунку: диспетчер уже знає | Кнопка SOS чітко пояснює — сповіщає диспетчера парку, не екстрені служби |
| 06.08 | Підключити водія — один клік з кабінету | Адмін видає доступ до застосунку прямо з кабінету, без зайвих кроків |
| 06.08 | Керуй своїм акаунтом сам | Водій сам скидає пароль і за потреби видаляє акаунт — без звернень у підтримку |
| 10.08 | Заробіток водія — тепер прозоріше | Розрахунок заробітку в застосунку доведено точний, без подвійного врахування |

### Наперед (демонструє відкладену публікацію, раз на 2-3 дні)

| Дата | Заголовок | Суть |
|---|---|---|
| 13.08 | Виплати водіям: журнал фактичних видач | В кабінеті власника — облік реально виданих водієві грошей, звірений з розрахунком |
| 15.08 | Одне повідомлення — і поїздка в системі | Запис ручної поїздки одним рядком у Telegram ("рука"), синхронізується автоматично |
| 18.08 | Пробіг і витрати — в одному місці | Історія пробігу та облік витрат водія прямо в застосунку |
| 22.08 | Акт стану авто при кожній зміні | Водій фотографує авто при прийомі/здачі зміни — менше спірних ситуацій |
| 25.08 | Передача авто за QR-кодом | Прийом/здача авто між водіями скануванням QR, без паперів |

Дати орієнтовні, легко зрушити.

## Імпорт у БД — зроблено

- Чернетки — `workspace/docs/articles/2026-08-backlog/` (13 файлів,
  frontmatter + `# Заголовок` + текст).
- Скопійовано в `app/park-react-spa/storage/app/blog-backlog-import/`.
- `App\Support\BlogBacklogImportSeeder` + міграція
  `2026_08_11_170000_seed_blog_backlog_articles.php` (той самий патерн, що
  й `BlogPostImportSeeder`) — імпортує, парсить frontmatter, синхронізує
  теги. Ідемпотентно.
- **Перевірено локально**: усі 13 статей у `blog_posts` з правильними
  заголовками/датами/статусами/тегами, повторний запуск не дублює.

### Деплой на продакшн

1. Скопіювати вміст `workspace/docs/articles/2026-08-backlog/` у
   `app/park-react-spa/storage/app/blog-backlog-import/` на сервері
   суперпарку (той самий принцип, що й для першого імпорту статей).
2. `php artisan migrate --force` — підхопить нову міграцію разом з рештою.
3. Прибрати `storage/app/blog-backlog-import/` після успішного імпорту.
4. Статті з датою в майбутньому (`13.08`-`25.08`) з'являться на лендінгу
   автоматично при черговій синхронізації, коли їхня дата публікації настане
   — саме так, як і задумано відкладеною публікацією.

## Вихідні дані

- **Остання опублікована стаття:** 12.07.2026, "Передали авто в іншу філію"
  (`vehicle-transfer-history`). Підтверджено і в БД (`blog_posts`), і в
  git-історії лендінгу — пропущених статей між публікаціями немає.
- **Вікно для наповнення:** 12.07.2026 (виключно) — 11.08.2026 (сьогодні).

## Методологічна примітка (важливо)

Git-історія за цей період — тонка й малоінформативна:
- `app/park-react-spa`: 18 комітів з 12.07, здебільшого "хотфікс"/пакетна
  робота по Uklon.
- `mobile/app`: лише 5 комітів, повідомлення типу "хотфікси"/"дрібні правки
  під публікацію".
- `mobile-user/app`: **git-репозиторію взагалі не знайдено в цій копії** —
  нуль даних, це прогалина, а не підтверджена відсутність роботи (див. нижче).

Головне джерело, яке дало реальну картину — `mobile/app/whatsnew/*.txt`:
власні реліз-ноути застосунку, вже клієнтським тоном, з датами git-додавання
файлів по кожному релізу.

---

## Кандидати з високою впевненістю

### Бекенд / адмінка (park-react-spa)

1. **Точніший розрахунок заробітку водія** — рефакторинг `DriverEconomyCalculator`
   (PR #72, злито 10.08.2026). Пов'язано з виправленнями подвійного
   врахування чайових/компенсації, зробленими сьогодні ж.
   Кут подачі: *заробіток, який бачить водій — тепер доведено правильний, а не оцінний.*

2. **Адмін видає водію доступ до застосунку прямо з кабінету** — 06.08.2026,
   переробка `DriverAuthController` + новий `DriverPasswordController::setPassword`.
   Кут подачі: *підключення водія до мобільного застосунку — один клік з офісу, без танців з OTP.*

3. **Модуль "Виплати водіям"** — 11.08.2026 (сьогодні), `DriverStaffPayoutsController`
   + таблиця `driver_payout_records` + повний мобільний UI для персоналу.
   Реальний журнал фактичних видач готівки водію, звірений з розрахованою
   сумою — вирішує "чи справді видали водієві те, що показав застосунок".
   Велика фіча (web + mobile) — імовірно, флагманська стаття циклу.

4. **Дані Uklon переїхали в окрему архівну базу** — 13-24.07.2026, кілька
   міграцій переносу логів/авто/водіїв/замовлень/бонусів Uklon в архів.
   Кут подачі: *легша й швидша синхронізація з Uklon — старі дані заархівовані, не видалені.*

### Мобільний застосунок водія — релізи в межах вікна (підтверджено whatsnew)

- **1.3.29** (14.07): виправлено перемикання парку/ролі — кнопка режиму
  персоналу і доступні ролі оновлюються автоматично, без повторного входу.
- **1.3.30** (реліз 24.07, ноутки переписані 10.08 після рев'ю App Store):
  виправлено видимість камери QR-сканера; **кнопка SOS тепер прямо каже, що
  сповіщає диспетчера парку, а не поліцію/швидку** — вартий окремої короткої
  статті про довіру/безпеку.
- **Самообслуговування акаунта** (06.08, переробка `Profile.tsx`): скидання
  пароля + видалення акаунта прямо в застосунку. Пов'язано з роботою по
  Guideline 5.1.1(v) Apple. Кут подачі: *ти сам керуєш своїм акаунтом.*

### Блок "Запуск мобільного застосунку" (окремий пул статей)

Підтверджені окремі можливості водійського застосунку (частина — саме в
межах вікна, частина — трохи раніше, див. прогалину №2 нижче):
графік роботи (місяць/2 тижні/тиждень), офлайн-режим із кешем даних,
список авто парку з фільтрами, ручний запис поїздки ("рука") з синком у
Telegram, історія пробігу, облік витрат, фотодокази обслуговування,
акти стану авто, QR-передача авто, компонент відображення номерного знаку,
перемикання парк/роль (водій+персонал), і тепер виплати водіям.
Це 8-10 окремих статей про конкретні можливості, якщо робити повний цикл.

---

## Слабкі/непідтверджені кандидати — потребують твого підтвердження

- **"Кілька правок скоупу парку" (09.08)** — торкнулось `VehicleMileageUpdate.php`,
  також міграція видалення "секції інформації про викрадення". Незрозуміло
  з коду, чи це клієнтське, чи суто прибирання/фікс. Що таке "секція про
  викрадення" і чому прибрали — не знаю з коду.
- **"Оновлений підхід до автівок" (12.07)** — дедублікація авто + унікальні
  QR-токени. Можна подати як "QR-коди авто тепер завжди унікальні", але
  слабкі докази, що це взагалі помітно клієнту, а не тиха фіксація якості даних.
- **Аналітика використання і crash-reporting (24.07)** — внутрішній
  ops-інструментарій (BetterStack), не клієнтське. Імовірно пропустити або
  згадати побіжно в статті про стабільність.

## Прогалини — вирішено користувачем

1. **mobile-user (застосунок клієнта/пасажира)** — прибрано з плану.
   Git-репозиторію немає в цій копії, усі файли з датою модифікації
   23-24.06 — стара/неактуальна копія, до задачі не стосується.

2. **Позиціонування мобільного застосунку — ЛИШЕ для водія** (явна вказівка
   користувача: "функції персоналу поки замовчуємо"). Android уже
   опубліковано, iOS зараз на рев'ю Apple (той самий процес, яким займались
   сьогодні).

   Наслідок для циклу "запуск мобільного застосунку" — прибрати/не
   згадувати функції режиму персоналу:
   - перемикання роль водій↔персонал (мультипарк/подвійна роль) — НЕ згадувати;
   - мобільний UI "Виплати водіям" — це персональська сторінка, НЕ для цього циклу;
   - "Список автомобілів парку для персоналу" (з чернетки `2026-07-changelog.md`) — НЕ використовувати.

   Модуль "Виплати водіям" як тема лишається — але тільки як **бекенд/адмінка**
   стаття (про кабінет власника/менеджера), без згадки мобільного інтерфейсу
   персоналу.

   Оновлений список тем для циклу "запуск застосунку" (тільки водійські
   можливості): графік роботи, офлайн-режим, ручний запис поїздки ("рука"),
   історія пробігу, облік витрат, фотодокази обслуговування, акти стану авто,
   QR-передача авто, відображення номерного знаку, SOS-кнопка (диспетчер, не
   поліція), самообслуговування акаунта (скидання пароля/видалення).

## Наступні кроки

1. Написати повний список статей (заголовок + дата публікації + суть) на
   основі оновленого скоупу вище — показати користувачу на затвердження.
2. Написати повні тексти (клієнтський тон, без фантазій).
3. Завантажити в `blog_posts` через сьогоднішню CMS.
